{
  "buildCommand": "mkdir -p dist && npx html-minifier-terser --collapse-whitespace --remove-comments --minify-css true --minify-js true index.html -o dist/index.html && cp favicon.png dist/favicon.png",
  "outputDirectory": "dist"
}
